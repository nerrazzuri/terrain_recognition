"""x2_terrain_perception — Phase 1 perception pipeline (perception only).

Pure-logic algorithms live in the ``core`` subpackage (no ROS2 dependency, unit-tested);
the node modules are thin ROS2 wrappers around them.
"""
